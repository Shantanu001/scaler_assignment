const express = require("express");
const { route } = require("../app");
const router = express.Router();

const {findServer} = require("../controllers/mainController");


//mapping routes to corresponding function
router.route("/findServer").post(findServer);


module.exports = router;
